CREATE TABLE feedback (
                          id INTEGER PRIMARY KEY AUTOINCREMENT,
                          store_name TEXT,
                          feedback_text TEXT,
                          image_url TEXT,
                          created_at DATETIME
);
